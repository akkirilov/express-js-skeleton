<!DOCTYPE html>
<html lang="en">

    {{> head}}

    <body>
        {{> nav}}
        <div class="container">
            {{#if message}}
                <div class="message">{{message}}</div>
            {{/if}}
            <h1>Welcome to passport demo!</h1>
            <h3>Welcome, {{username}}</h3>
        </div>

    </body>
</html>
